apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qairon.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "qairon.name" . }}
    helm.sh/chart: {{ include "qairon.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "qairon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
