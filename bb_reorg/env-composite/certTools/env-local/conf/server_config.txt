 [ req ]

 distinguished_name     = req_distinguished_name
 prompt                 = no

 [ req_distinguished_name ]
 C                      = US
 ST                     = CA
 L                      = Palo Alto
 O                      = StayUp
 CN                     = service-gateway
 emailAddress           = admin@stayup.zone

 [ ca ]
 default_ca      = CA_default            # The default ca section

 [ CA_default ]

 dir            = .          # top dir
 database       = $dir/server/index.txt # index file.
 new_certs_dir  = $dir/server           # new certs dir
 
 certificate    = $dir/ca/ca.crt        # The CA cert
 serial         = $dir/server/serial    # serial no file
 private_key    = $dir/ca/ca.key        # CA private key
 RANDFILE       = $dir/server/.rand     # random number file
 
 default_days   = 3650                  # how long to certify for
 default_crl_days= 30                   # how long before next CRL
 default_md     = sha256                # force sha256, md5 is not accepted by latest JVM (as of 1.8u92)

 policy         = policy_any            # default policy
 email_in_dn    = no                    # Don't add the email into cert DN

 name_opt       = ca_default            # Subject name display option
 cert_opt       = ca_default            # Certificate display option
 copy_extensions = none                 # Don't copy extensions from request

 [ policy_any ]
 countryName            = supplied
 stateOrProvinceName    = optional
 organizationName       = optional
 organizationalUnitName = optional
 commonName             = supplied
 emailAddress           = optional
